# Open Questions

- Can guest customers save a method before account creation?
- Which payment provider tokenization constraints must be reflected in the UI?
- Should customers be able to set a default payment method?
